extends layout

block content
  h1 ID: #{instance_book._id}
  h1 #{title}: #{instance_book.name}

  p Do you really want to delete this Book Instance?

  form(method='POST' action='')
      div.form-group
      input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=instance_book._id )

      button.btn.btn-primary(type='submit') Delete